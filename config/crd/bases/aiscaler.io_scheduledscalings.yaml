---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.19.0
  name: scheduledscalings.aiscaler.io
spec:
  group: aiscaler.io
  names:
    kind: ScheduledScaling
    listKind: ScheduledScalingList
    plural: scheduledscalings
    shortNames:
    - ss
    singular: scheduledscaling
  scope: Cluster
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.active
      name: Active
      type: boolean
    - jsonPath: .status.phase
      name: Phase
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1
    schema:
      openAPIV3Schema:
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: ScheduledScalingSpec defines the desired state of ScheduledScaling.
            properties:
              policy:
                description: SchedulePolicy defines what happens when the schedule
                  is active.
                properties:
                  maxReplicas:
                    format: int32
                    type: integer
                  minReplicas:
                    format: int32
                    type: integer
                  targetReplicas:
                    format: int32
                    type: integer
                type: object
              schedule:
                description: ScheduleSpec defines when this schedule activates.
                properties:
                  endTime:
                    format: date-time
                    type: string
                  expression:
                    description: Natural language expression (compiled by LLM)
                    type: string
                  recurrence:
                    description: Cron expression (for Recurrence)
                    type: string
                  startTime:
                    description: One-shot activation window
                    format: date-time
                    type: string
                  timezone:
                    default: UTC
                    description: Timezone (IANA), default UTC
                    type: string
                  type:
                    description: ScheduleType defines the type of schedule.
                    enum:
                    - Recurrence
                    - OneShot
                    - NaturalLanguage
                    type: string
                required:
                - type
                type: object
              targetRef:
                properties:
                  apiVersion:
                    default: apps/v1
                    type: string
                  kind:
                    default: Deployment
                    enum:
                    - Deployment
                    - StatefulSet
                    - Rollout
                    type: string
                  name:
                    type: string
                  namespace:
                    type: string
                required:
                - name
                - namespace
                type: object
              ttlAfterCompletion:
                description: TTL after which completed one-shot schedules are cleaned
                  up
                type: string
            required:
            - policy
            - schedule
            - targetRef
            type: object
          status:
            description: ScheduledScalingStatus defines the observed state.
            properties:
              active:
                type: boolean
              compilationConfidence:
                type: number
              compiledFrom:
                description: Compiled schedule (only for NaturalLanguage type)
                type: string
              lastActivation:
                format: date-time
                type: string
              nextActivation:
                format: date-time
                type: string
              phase:
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
